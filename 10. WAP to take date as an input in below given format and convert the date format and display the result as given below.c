#include <stdio.h>
int main()
{
    int d, m, y;
    printf("DD = Date, MM = Month, YYYY = Year\n");
    scanf("%d", &d);
    scanf("%d", &m);
    scanf("%d", &y);
    printf("Day _ %d, Month _ %d, Year _ %d", d, m, y);
}